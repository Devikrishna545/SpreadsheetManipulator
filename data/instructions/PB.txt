Deployment Bugs to Solve
=========================
=========================

✅ When a set of commands is uploaded, and a command is executed (passes through the prompt execution pipeline of checking if script already exists, sandbox testing, fixing and running) and the command to be executed is not executed successfully after all 5 attempts of fixing (automatically and manually), use the Gemini Advanced method, for generating complex script (you can find it in the #file:llm_service.py, file within the #sym:_generate_complex_script method) for the 6th and 7th attempts.
For some reason the none of the information about token consumption displayed in the terminal is correct. The token consumption is always showing zero, whether for complex or simple script generations it always shows zero tokens used and low consumption.
✅ Delete all unnecessary buttons code from front to backend ("generateActionPlanBtn","updateSchemaBtn","transformSchemaBtn").
Add a UI element on the left of the "statusBadge" to show token consumption details.
Add an aesthetic margin whith line numbers to the commands view section, for each line of text extracted from the commands file, mapped with the spreadsheet.
When app is launched all particles start moving into the screen at the same time. Particles should move into the screen at different intervals and randomly.
When  hovering over the menu button it moves up and down. Let the menu button remain in one place on hover. Don't change anything else, just remove the hover effect.
Within the mapping manager, when the edit button is clicked, an modal error modal which says "Error Edit functionality coming soon" is shown. The commands view is suppose to be shown, with edit options to edit the commands.
When the delete button is clicked, the modal for delete appears behind the "Manage Mapping" modal. The dlete mapping modal is a subset of the "Manage Mapping" modal and is suppose to appear infront of the mapping manage modal, not behind it.
The "statusBadge" on the top right is suppose to align itself with all the other components below it. The right side of this component shouls align itself with all other components below it.
The size of all components should fit between the header bar and the footer bar of the HTML page. The main page UI (spreadsheet table) and the sidebar are overflowing below the footer and header bars.
While resizing the "AI Command Interface" there is a massive delay between when the user stretches open the text area, and when the text area actually open. It's amlost like the text aread has an innertia. Modify that element's resize functionality to be more fluid and responsive.
While the spreadsheet is loaded into view, the mouse should not interact with particles in the background to avoid distracting the user.
When there are multiple lines of text within the "AI Command Interface" (whether through text wrapping or new lines) and the "Up" button s pressed, it immediately switches the text box into applying file history, which is bad for the user, because if the try to move between lines with the up or down key, they get their typed command removed. Modify the text input to switch to history if and only if the user presses the "Alt+Up" key combination or "Alt+Down".
Move all the buttons of the "actionsSection" on the left side bar, into the "spreadsheet-header", and place them to be like all other buttons already placed there, then delete the "actionsSection" on the left side bar.
Add 2 gears rotating on each other between the "Auto" and the "Editor" text on the footer bar.
Add JS animated usermanual page, giving users complete instructions on how to use the app.
CUDA not available using CPU version, error, while Cuda is properly installed.
Add column and row header highlighting to form a cell, when handsonTable selection is over that call.
# Enter button should use an enter icon, not a pencil icon
# Create another HTML page with a well formated usermanual of the app, with gifs. (The new HTML page should use same design as the index.html page). Add all error messages, and how to resolve them (from a user perspective)
# Add light/dark mode toggle (dark mode by default). Modes persist across page refresh and app restarts
# Remove particle-mouse interaction, when there is an element between the mouse and the particles (if the z-index of the element is lower than z-index of the particles stop the mouse-particle interaction).

Handle app security.
Clean all code for deployment.
When all things are working fine, convert Azure VM Deployment.md into script which will ask me for each major step (step 1, 2 etc...), if it should execute or not.

ALGORITHM_KEEP_ESSENTIAL_DATA

// Preprocess
-> Remove
--> Delete all empty rows.
--> Delete all empty columns.

// Keep
-> Tag table headers to keep.
--> Create a new column called [] to accomodate account numbers.
--> Create a new column called [] to accomodate account names.
--> Move all account names to their corresponding cells under the [] column. An account name is any string in a cell found in [] containing [].
--> Move all account numbers to their corresponding cells under the [] column. An account number is any [] digit number in any cell under the [] column. Examples of account numbers are [], [] and [] etc.
--> Create new top row to store table headers.
--> Move headers into newly created rows.
--> Create new rows to accomodate new header content.
--> Move header content into newly created rows.

// Delete
Delete non tagged rows.
Delete non tagged columns.
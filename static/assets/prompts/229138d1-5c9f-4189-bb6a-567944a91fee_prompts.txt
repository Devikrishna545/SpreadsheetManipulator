I have an Excel file that contains ledger transaction data with multiple groups. Each group has a header containing Account Number and Account Name followed by transaction rows. The file also contains subtotal rows, grand totals, repeated column headers inside the data, blank rows, and some irrelevant metadata columns. Please process this file with the following steps: 1. Identify and extract Account Number and Account Name headers for each group. 2. Repeat these group header values on every transaction row belonging to that group. 3. Remove all subtotal, grand total, and blank rows. 4. Remove all repeated table header rows or column titles that appear within the data rows (for example rows containing “Voucher”, “Posting type”, “Ledger account”, “Description”, etc.). 5. Remove all rows where both the Voucher column is empty or missing AND all amount columns (such as Debit, Credit, or Amount in accounting currency) are zero, empty, or missing. 6. Remove irrelevant metadata columns such as “Page 1 of 1” or timestamps. 7. Keep all other columns in their original order. 8. Keep all rows where there is any meaningful data. 9. Preserve all date columns as datetime types. Return the cleaned data as an Excel file with: • Each transaction row repeated with its corresponding Account Number and Account Name. • All subtotal, total, blank, and repeated header rows removed. • Columns and rows cleaned and ordered as per original data (excluding metadata). If you need the file, I will upload it now.
1. Identify and extract Account Number and Account Name headers for each group. 2. Repeat these group header values on every transaction row belonging to that group. 3. Remove all subtotal, grand total, and blank rows. 4. Remove all repeated table header rows or column titles that appear within the data rows (for example rows containing “Voucher”, “Posting type”, “Ledger account”, “Description”, etc.). 5. Remove all rows where both the Voucher column is empty or missing AND all amount columns (such as Debit, Credit, or Amount in accounting currency) are zero, empty, or missing. 6. Remove irrelevant metadata columns such as “Page 1 of 1” or timestamps. 7. Keep all other columns in their original order. 8. Keep all rows where there is any meaningful data. 9. Preserve all date columns as datetime types. 10. Each transaction row repeated with its corresponding Account Number and Account Name. 11. All subtotal, total, blank, and repeated header rows removed. 12. Columns and rows cleaned and ordered as per original data (excluding metadata).
